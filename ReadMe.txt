install python
use commands:
    -python -m venv llama3_env - to create a virtual enviroment
    -llama3_env\Scripts\activate (make sure policys are active (Set-ExecutionPolicy -ExecutionPolicy AllSigned -Scope CurrentUser))
install Libraries
    -pip install torch torchvision torchaudio (PyTorch)
    -pip install transformers datasets (Hugging Face Transformers and Datasets)

Run test.py

